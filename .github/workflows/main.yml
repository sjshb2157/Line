name: Build MediaPipe SO

on:
  workflow_dispatch:
    inputs:
      mediapipe_version:
        description: 'MediaPipe 版本 tag (v0.10.32 是最后一个保留 vision_jni 旧名的版本)'
        required: true
        default: 'v0.10.32'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: 安装依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip git unzip zip curl wget \
            openjdk-17-jdk build-essential binutils
          python3 -m pip install --user numpy

      - name: 安装 Bazel 6.5.0
        run: |
          curl -fsSL https://bazel.build/bazel-release.pub.gpg \
            | gpg --dearmor > bazel.gpg
          sudo mv bazel.gpg /etc/apt/trusted.gpg.d/
          echo "deb [arch=amd64] https://storage.googleapis.com/bazel-apt stable jdk1.8" \
            | sudo tee /etc/apt/sources.list.d/bazel.list
          sudo apt-get update && sudo apt-get install -y bazel-6.5.0
          sudo ln -sf /usr/bin/bazel-6.5.0 /usr/bin/bazel
          bazel --version

      - name: 安装 Android SDK 35 和 NDK r28b
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$JAVA_HOME/bin:$PATH

          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q commandlinetools-linux-11076708_latest.zip -d cmdtools
          mkdir -p $HOME/android-sdk/cmdline-tools
          mv cmdtools/cmdline-tools $HOME/android-sdk/cmdline-tools/latest

          export ANDROID_HOME=$HOME/android-sdk
          export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin

          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "ndk;28.1.13356709"

          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$HOME/android-sdk/ndk/28.1.13356709" >> $GITHUB_ENV
          echo "JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" >> $GITHUB_ENV

      - name: Clone MediaPipe 源码
        run: |
          git clone --depth 1 \
            --branch ${{ github.event.inputs.mediapipe_version }} \
            https://github.com/google-ai-edge/mediapipe.git

      - name: 配置 WORKSPACE
        run: |
          cd mediapipe
          cat >> WORKSPACE <<'EOF'

          android_sdk_repository(name = "androidsdk")
          android_ndk_repository(name = "androidndk", api_level = 21)
          bind(name = "android/crosstool", actual = "@androidndk//:toolchain")
          EOF

      - name: 编译 libmediapipe_tasks_vision_jni.so
        run: |
          cd mediapipe
          # v0.10.32 里这是 mediapipe_jni_binary 生成的 output target，产物直接落地
          # opencv 两个 define 必须留，否则运行时报 libopencv_java4.so not found
          bazel build -c opt \
            --config=android_arm64 \
            --define MEDIAPIPE_DISABLE_OPENCV=1 \
            --define EXCLUDE_OPENCV_SO_LIB=1 \
            //mediapipe/tasks/java/com/google/mediapipe/tasks/vision:libmediapipe_tasks_vision_jni.so

          echo "bazel-bin: $(readlink -f bazel-bin)"
          ls -lh bazel-bin/mediapipe/tasks/java/com/google/mediapipe/tasks/vision/libmediapipe_tasks_vision_jni.so
        timeout-minutes: 120

      - name: 收集产物 + 校验
        run: |
          cd mediapipe
          mkdir -p ../output
          SO=$(find -L bazel-bin -name "libmediapipe_tasks_vision_jni.so" 2>/dev/null | head -1)
          if [ -z "$SO" ]; then
            echo "::error::没找到产物，列出 vision 目录所有 .so："
            find -L bazel-bin -path "*tasks/vision*" -name "*.so" 2>/dev/null
            exit 1
          fi
          echo "产物: $SO"
          cp "$SO" ../output/

          cd ../output
          f=libmediapipe_tasks_vision_jni.so
          echo "=== file ===";            file "$f"
          echo "=== NEEDED (应无 opencv / 无 pairipcore) ==="
          readelf -d "$f" | grep NEEDED
          echo "=== NDK note ==="
          readelf -n "$f" 2>/dev/null | grep -iA1 android || true
          echo "=== 16K 对齐 (看 Align 列是否 2**14) ==="
          readelf -lW "$f" 2>/dev/null | grep -E "LOAD" | head -3
          echo "=== 导出 Java_ 符号 (和原库 diff 用) ==="
          nm -D --defined-only "$f" | grep "Java_" | awk '{print $NF}' | sort -u > "$f.symbols.txt"
          echo "符号数: $(wc -l < $f.symbols.txt)"

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: mediapipe-so-${{ github.event.inputs.mediapipe_version }}
          path: |
            output/*.so
            output/*.symbols.txt
          retention-days: 7
